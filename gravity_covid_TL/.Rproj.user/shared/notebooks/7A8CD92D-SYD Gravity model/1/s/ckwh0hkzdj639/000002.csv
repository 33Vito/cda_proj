"0","```r
allrank %>% 
  # mutate(oldrank = lbtsyd1$oldrank[.match(SA2, lbtsyd1$SA2)]) %>% 
  rowwise() %>% 
  # mutate(total = mean(c(oldrank, a.shops, a.hospitals, a.schools, a.transit, a.work), na.rm=T)) %>% 
  mutate(local = mean(c(a.shops, a.hospitals, a.schools, a.transit), na.rm=T)) %>% 
  ungroup() %>% 
  mutate(`30-min city index rankings` = min_rank(total)) %>% 
  mutate(`Local accessibility index rankings` = min_rank(local)) %>% 
  rename(`Work accessibility index rankings` = a.work) %>% 
  rename(`Liveability index rankings` = oldrank) %>% 
  select(SA2, `30-min city index rankings`, `Local accessibility index rankings`, 
         `Work accessibility index rankings`, `Liveability index rankings`) %>% 
  arrange(`30-min city index rankings`) %>% 
  .[1:50,] %>% 
  # copy.table()
  DT::datatable(extensions = 'Buttons',
                filter = \"top\",
                class = \"compact hover\")
```"
