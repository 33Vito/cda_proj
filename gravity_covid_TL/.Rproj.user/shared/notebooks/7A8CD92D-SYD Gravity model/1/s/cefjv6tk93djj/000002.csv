"0","```r
beneficiaries <- dd %>% 
    mutate(pred_gravity = pred_gt$gravity[.match(SA2, pred_gt$SA2)]) %>% 
    mutate(growth_gravity = pred_gravity/gravity - 1) %>% filter(growth_gravity > 0) %>% .[[\"SA2\"]]
live %>% 
  group_by(area %in% beneficiaries) %>% 
  summarise(count = sum(count)) %>% 
  ungroup() %>% 
  mutate(percent = count/sum(count))
```"
