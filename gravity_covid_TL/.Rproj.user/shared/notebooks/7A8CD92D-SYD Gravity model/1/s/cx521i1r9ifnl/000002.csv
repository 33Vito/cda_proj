"0","```r
gravity(bind_rows(trains %>% select(lon, lat), 
                  lightrails %>% select(lon, lat), 
                  ferries %>% select(lon, lat)), 
        dcut = 2.5, md = \"Cycling\") %>% 
  ggplot() +  
  geom_map(inherit.aes = FALSE, alpha=.85,
           aes(map_id = id, fill=-rank), map = SYD_tidy, col=\"grey50\", size=.1) +
  geom_point(data = bind_rows(trains %>% select(lon, lat), 
                              lightrails %>% select(lon, lat), 
                              ferries %>% select(lon, lat)),
             aes(x=lon, y=lat),
             size=1, col=\"orange\", alpha=.2) +
  # stat_density2d(data = rbind(sschools %>% select(Long, Lat),
  #                         pschools %>% select(Long, Lat)), 
  #            aes(x=Long, y=Lat, fill=..level.., alpha=..level..), 
  #            binwidth = 1.2,
  #            geom=\"polygon\") +
  expand_limits(x = SYD_tidy$long, y = SYD_tidy$lat) +
  coord_cartesian(xlim = c(150.7, 151.48), ylim = c(-34.1, -33.5)) +
  xlim(150.7,151.48) + ylim(-34.1,-33.5) +
  scale_fill_gradient(low=\"white\", high=\"orange\") + 
  scale_alpha_continuous(range = c(0,.3)) + 
  theme_void() +
  theme(legend.position = \"right\")
```"
