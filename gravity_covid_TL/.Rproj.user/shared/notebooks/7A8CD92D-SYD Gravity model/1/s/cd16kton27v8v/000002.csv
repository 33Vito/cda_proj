"0","```r
SYD_dt %>% 
  # filter(SA2 %in% pop_SYD$`SA2 (UR)`) %>% 
  mutate(rank = allrank$newrank[.match(SA2, allrank$SA2)]) %>% 
  arrange(rank) %>% 
  group_by(rank <= 50) %>% 
  summarise(n = n(), area = sum(area)) %>% 
  mutate(percent = area/sum(area)) %>% 
  add_row(n = sum(.$n), area = sum(.$area), percent = sum(.$percent))
```"
